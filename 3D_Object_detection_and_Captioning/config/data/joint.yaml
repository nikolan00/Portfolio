# Managed by Hydra

dataset_root_path: ${project_root_path}/data
dataset: JointDataset
dataset_path: ${data.dataset_root_path}/joint_data
scanrefer_path: ${data.dataset_root_path}/scanrefer
scans_path: ${data.dataset_root_path}/scannetv2/scans


batch_size: 12
num_workers: 8

instance_size: 48
